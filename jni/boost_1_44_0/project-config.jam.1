# Boost.Build Configuration
# Automatically generated by bootstrap.sh

import option ;
import feature ;

# Compiler configuration. This definition will be used unless
# you already have defined some toolsets in your user-config.jam
# file.
if ! gcc in [ feature.values <toolset> ]
{
    using gcc : : arm-linux-androideabi-g++ ;
}

project : default-build <toolset>gcc ;

# List of --with-<library> and --without-<library>
# options. If left empty, all libraries will be built.
# Options specified on the command line completely
# override this variable.
libraries =  --without-date_time --without-filesystem --without-graph --without-graph_parallel --without-math --without-mpi --without-python --without-signals --without-test --without-thread --without-wave ;

# These settings are equivivalent to corresponding command-line
# options.
option.set prefix : /opt/android-ndk-r4/build/platforms/android-8/arch-arm/usr//local ;
option.set exec-prefix : /opt/android-ndk-r4/build/platforms/android-8/arch-arm/usr//local ;
option.set libdir : /opt/android-ndk-r4/build/platforms/android-8/arch-arm/usr//local/lib ;
option.set includedir : /opt/android-ndk-r4/build/platforms/android-8/arch-arm/usr//local/include ;
