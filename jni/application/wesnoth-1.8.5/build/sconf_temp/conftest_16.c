
        #include <SDL_ttf.h> 
        
#if SDL_VERSIONNUM(SDL_TTF_MAJOR_VERSION, SDL_TTF_MINOR_VERSION, SDL_TTF_PATCHLEVEL) < SDL_VERSIONNUM(2, 0, 8)
#error Library is too old!
#endif

        int main(int argc, char** argv)
        {
        }
        
